{
  "name": "crypto-lab-salamander",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Key commitment in AEAD — one AES-GCM ciphertext, two keys, two valid plaintexts. A browser teaching demo of the invisible-salamanders attack.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:a11y": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "typescript": "~7.0.2",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
